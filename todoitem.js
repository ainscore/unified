define(["require"], function(require) {

    return function(db) {
        var TodoItem = function() {
            this.name = "";
            this.deadline = ;
        };

        TodoItem.prototype = {
        }

        TodoItem.getByUser = function(userId) {

        }

        TodoItem.find = function(id) {

        }

        return TodoItem;
    }

});
